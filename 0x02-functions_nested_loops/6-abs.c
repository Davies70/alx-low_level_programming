#include "main.h"

/**
 * _abs - Short description, reverse signs
 * @r: interger variable
 *
 * Return: Always 0 (Success)
 */

int _abs(int r)
{
	if (r < 0)
	{
		return (-1 * r);
	}
	else
		return (r);

}
